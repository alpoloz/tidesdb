# Next Steps

- Add Bloom filters to SSTables for faster negative lookups.
- Group records into blocks inside SSTables (with block index and restart points).
- Add a versioned footer/header to SSTables to support format evolution.
- Explore compaction strategies (size-tiered, leveled, or hybrid).
